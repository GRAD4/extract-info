# Embedding model (any sentence-transformers compatible model)
EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2

# Default search radius in km when none is specified in the query
DEFAULT_RADIUS_KM=50
